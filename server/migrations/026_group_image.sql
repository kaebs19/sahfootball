-- 026 — صورة المجلس.
--
-- عمود واحد بنفس عقد users.avatar_url: مسار تحت /uploads/ يقدّمه
-- السيرفر، والملف على القرص الدائم (راجع utils/avatarFile). لا
-- جدول وسائط ولا أحجام متعددة: صورة واحدة مربّعة تُعرض في البطاقة
-- والترويسة ومعاينة رابط الدعوة، والقصّ في العميل قبل الرفع.
ALTER TABLE groups ADD COLUMN IF NOT EXISTS image_url TEXT;
