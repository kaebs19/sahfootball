-- ٠٤١ — إسقاط عمودٍ استُبدل بجدول.
--
-- wildcards_used أُضيف في ٠٣٩ ثم حلّ محلّه جدول fantasy_chips في
-- ٠٤٠ (رقاقةٌ في صفّ لا رقاقةٌ في عمود). وتركُه لا يضرّ اليوم
-- ويُربك بعد شهر: من يقرأ الجدول يجد مصدرين لحقيقةٍ واحدة ولا
-- يعرف أيّهما يُصدَّق.
ALTER TABLE fantasy_squads DROP COLUMN IF EXISTS wildcards_used;
